/**
 * Palette presets — the palette is strictly ONE tile per type (user direction 2026-07-02): identity
 * tiles come free from palettePresets(), profile/cap variants (Dome/Cone/Crater, Wedge/Fillet,
 * Cylinder/Capsule/Frustum) are Inspector dropdowns, and even geometry templates (Pyramid/Box) were
 * dropped — reshape a Plateau's rings instead. No presets are defined here today; definePreset stays
 * for user-saved presets (project.lambert) and any future template that earns a tile.
 * Imported for its side effects by field/objects/index.ts.
 */
export {};
