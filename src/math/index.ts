// Vendored from carapace's (now aphralatrax's) primitives package, 2026-07-13 — lambert is a
// public app and must not depend on the private engine-mirror repo, and it only ever needed
// these two value types. Self-contained; upstream sync is manual and deliberately unlikely.
export { Vector2 } from "./Vector2";
export { Vector3 } from "./Vector3";
